PORT=5000

# MongoDB connection
MONGODB_URI=mongodb://127.0.0.1:27017/
MONGODB_DB_NAME=lpr

# Search engine mode
SEARCH_ENGINE=simple

# Gemini settings
# Keep real key value only in backend/.env
GEMINI_API_KEY=your_api_key_here
EMBEDDING_MODEL=models/embedding-001
LLM_MODEL=gemini-2.5-flash-lite
